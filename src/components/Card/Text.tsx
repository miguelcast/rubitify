import styled from 'styled-components';

const Text = styled.span`
  overflow: hidden;
  white-space: nowrap;
  width: 110px;
  font-size: 0.8rem;
  font-style: normal;
  text-overflow: ellipsis;
  text-align: center;
`;

export default Text;
